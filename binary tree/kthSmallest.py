

class Solution:
  def kthSmallest(self, root: Optional[TreeNode])-> bool:
    if not root: return True
    q.deque([(root, float("-inf"), float("inf")])
    while q:
      node, left, right = q.popleft()
      if not ()
